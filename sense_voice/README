# 开源sense_voice 的配置AND使用示例
## 环境要求

- **操作系统**: CentOS 7（或其他兼容的 Linux 系统）
- **Python 版本**: 3.7 及以上
- **Docker**: 20.10 及以上（推荐使用 Docker 构建环境）
- **网络连接**: 需要访问外部仓库以下载依赖包

## 安装步骤

### 1. 使用 Docker 构建镜像

推荐使用 Docker 来构建和运行 `sense_voice` 项目环境，这样可以避免系统环境之间的依赖冲突。

